const { getDifficultyConfig, createSeededRNG } = require('../dist-test/src/utils/difficultyConfig');
// Since our source files are TypeScript, let's write a node script or compile with ts-node/esbuild.
